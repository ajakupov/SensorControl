# SensorControl

A simple application to activate/deactivate sensors via the RESTful API. 
Contains simple loging and http connector in order to send POST request and get the JSON response.

The sensor status is also visualized.
